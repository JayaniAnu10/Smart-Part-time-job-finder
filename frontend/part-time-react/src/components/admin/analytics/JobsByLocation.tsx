import { Card, CardHeader, CardTitle } from "@/components/ui/card";

export default function JobsByLocation() {
  return (
    <Card className="rounded-2xl w-full h-[300px] max-w-full">
      <CardHeader className="p-4 sm:p-6 md:p-8">
        <CardTitle className="text-xl sm:text-left font-medium">Jobs by Location</CardTitle>
      </CardHeader>

      
    </Card>
  );
}
